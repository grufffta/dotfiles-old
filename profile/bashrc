# Set bash to work with VI commands
set -o vi on

# source ~/.config/profile/bash_aliases
# Load prompt configuration
source ~/dotfiles/profile/shell_prompt.sh
# create temp files (WSL)
# sudo systemd-tmpfiles --create &>/dev/null
